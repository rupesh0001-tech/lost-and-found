<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Contact Details - LoFo</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Inter', sans-serif;
    }

    body {
      background: #f5f5f7;
      position: relative;
      overflow-x: hidden;
      min-height: 100vh;
    }

    /* Decorative background pattern */
    body::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 100%;
      background-image: 
        radial-gradient(circle at 20% 50%, rgba(59, 130, 246, 0.03) 0%, transparent 50%),
        radial-gradient(circle at 80% 80%, rgba(147, 197, 253, 0.04) 0%, transparent 50%);
      pointer-events: none;
      z-index: 0;
    }

    /* Diagonal grid pattern */
    body::after {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 100%;
      background-image: 
        repeating-linear-gradient(45deg, transparent, transparent 50px, rgba(0,0,0,0.01) 50px, rgba(0,0,0,0.01) 51px);
      pointer-events: none;
      z-index: 0;
    }

    header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 20px 60px;
      background: rgba(255, 255, 255, 0.8);
      backdrop-filter: blur(10px);
      -webkit-backdrop-filter: blur(10px);
      position: relative;
      z-index: 100;
    }

    .logo {
      font-weight: 700;
      font-size: 24px;
      color: #000;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .logo::before {
      content: '';
      width: 8px;
      height: 8px;
      background: #3b82f6;
      border-radius: 50%;
    }

    .logo::after {
      content: '';
      width: 8px;
      height: 8px;
      background: #60a5fa;
      border-radius: 50%;
    }

    nav ul {
      display: flex;
      list-style: none;
      gap: 40px;
    }

    nav ul li a {
      text-decoration: none;
      color: #000;
      font-weight: 500;
      font-size: 15px;
      transition: color 0.3s;
    }

    nav ul li a:hover {
      color: #3b82f6;
    }

    .auth-buttons {
      display: flex;
      gap: 12px;
    }

    .logout {
      padding: 10px 24px;
      border-radius: 8px;
      text-decoration: none;
      font-weight: 500;
      font-size: 14px;
      background: #3b82f6;
      color: #fff;
      transition: all 0.3s;
    }

    .logout:hover {
      background: #2563eb;
      transform: translateY(-1px);
      box-shadow: 0 4px 12px rgba(59, 130, 246, 0.3);
    }

    .page-title {
      text-align: center;
      font-size: 42px;
      font-weight: 700;
      color: #000;
      margin: 3rem 0 1rem;
      letter-spacing: -0.02em;
      position: relative;
      z-index: 1;
    }

    .page-subtitle {
      text-align: center;
      font-size: 16px;
      color: #6b7280;
      margin-bottom: 3rem;
      position: relative;
      z-index: 1;
    }

    .container-custom {
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: calc(100vh - 400px);
      padding: 20px;
      position: relative;
      z-index: 1;
    }

    .contact-card {
      width: 100%;
      max-width: 700px;
      border: 1px solid #e5e7eb;
      border-radius: 20px;
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.08);
      background-color: #ffffff;
      transition: all 0.3s ease;
    }

    .contact-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 12px 40px rgba(0, 0, 0, 0.12);
    }

    .card-body {
      padding: 3rem 2.5rem;
    }

    .user-avatar {
      width: 100px;
      height: 100px;
      background: linear-gradient(135deg, #3b82f6 0%, #60a5fa 100%);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 48px;
      color: #fff;
      margin: 0 auto 2rem;
      font-weight: 700;
    }

    .card-title {
      font-weight: 700;
      color: #000;
      font-size: 28px;
      margin-bottom: 2rem;
      text-align: center;
      letter-spacing: -0.02em;
    }

    .contact-info {
      background: #f9fafb;
      border-radius: 12px;
      padding: 1.5rem;
      margin-bottom: 1.25rem;
      border: 1px solid #e5e7eb;
    }

    .info-label {
      font-size: 13px;
      font-weight: 600;
      color: #6b7280;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      margin-bottom: 0.5rem;
    }

    .info-content {
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 1rem;
    }

    .info-value {
      font-size: 18px;
      color: #000;
      font-weight: 500;
      word-break: break-all;
    }

    .copy-btn {
      background-color: #3b82f6;
      color: white;
      border: none;
      padding: 8px 16px;
      border-radius: 8px;
      font-size: 14px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s ease;
      white-space: nowrap;
      box-shadow: 0 2px 8px rgba(59, 130, 246, 0.2);
    }

    .copy-btn:hover {
      background-color: #2563eb;
      transform: translateY(-2px);
      box-shadow: 0 4px 12px rgba(59, 130, 246, 0.3);
    }

    .copy-btn:active {
      transform: translateY(0);
    }

    .toast {
      position: fixed;
      top: 100px;
      right: 20px;
      background: #10b981;
      color: white;
      padding: 14px 20px;
      border-radius: 10px;
      box-shadow: 0 4px 16px rgba(16, 185, 129, 0.3);
      font-weight: 600;
      font-size: 14px;
      z-index: 1000;
      animation: slideIn 0.3s ease, slideOut 0.3s ease 2.7s;
      opacity: 0;
      pointer-events: none;
    }

    .toast.show {
      opacity: 1;
      pointer-events: auto;
    }

    @keyframes slideIn {
      from {
        transform: translateX(400px);
        opacity: 0;
      }
      to {
        transform: translateX(0);
        opacity: 1;
      }
    }

    @keyframes slideOut {
      from {
        transform: translateX(0);
        opacity: 1;
      }
      to {
        transform: translateX(400px);
        opacity: 0;
      }
    }

    @media (max-width: 768px) {
      header {
        padding: 16px 20px;
      }

      nav ul {
        display: none;
      }

      .page-title {
        font-size: 32px;
        margin: 2rem 0 0.5rem;
      }

      .page-subtitle {
        font-size: 14px;
        margin-bottom: 2rem;
      }

      .contact-card {
        max-width: 95%;
      }

      .card-body {
        padding: 2rem 1.5rem;
      }

      .card-title {
        font-size: 24px;
      }

      .info-value {
        font-size: 16px;
      }

      .info-content {
        flex-direction: column;
        align-items: stretch;
      }

      .copy-btn {
        width: 100%;
      }

      .toast {
        right: 10px;
        left: 10px;
        top: 80px;
      }
    }
  </style>
</head>

<body>

  <header>
    <div class="logo">LoFo</div>
    <nav>
      <ul>
        <li><a href="/home">Home</a></li>
        <li><a href="/found">Found</a></li>
        <li><a href="/lost">Lost</a></li>
        <li><a href="/report">Check Reports</a></li>
      </ul>
    </nav>
    <div class="auth-buttons">
      <a href="/Logout" class="logout">Logout</a>
    </div>
  </header>

  <h2 class="page-title">Contact Details</h2>
  <p class="page-subtitle">Connect with the item owner to arrange pickup</p>

  <div class="container-custom">
    <div class="contact-card">
      <div class="card-body">
        <div class="user-avatar">
          <%= newUser.name.charAt(0).toUpperCase() %>
        </div>
        <h5 class="card-title"><%= newUser.name %></h5>

        <div class="contact-info">
          <div class="info-label">Phone Number</div>
          <div class="info-content">
            <span class="info-value" id="phone"><%= newUser.phone %></span>
            <button class="copy-btn" onclick="copyToClipboard('phone', 'Phone')">Copy</button>
          </div>
        </div>

        <div class="contact-info">
          <div class="info-label">Email Address</div>
          <div class="info-content">
            <span class="info-value" id="email"><%= newUser.email %></span>
            <button class="copy-btn" onclick="copyToClipboard('email', 'Email')">Copy</button>
          </div>
        </div>
      </div>
    </div>
  </div>

  <div id="toast" class="toast"></div>

  <%- include('partials/footer') %>

  <script>
    function copyToClipboard(id, label) {
      const text = document.getElementById(id).innerText;
      navigator.clipboard.writeText(text).then(() => {
        showToast(`${label} copied to clipboard!`);
      }).catch(() => {
        showToast('Failed to copy');
      });
    }

    function showToast(message) {
      const toast = document.getElementById('toast');
      toast.textContent = message;
      toast.classList.add('show');
      
      setTimeout(() => {
        toast.classList.remove('show');
      }, 3000);
    }
  </script>

</body>

</html>