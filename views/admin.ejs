<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>LoFo - Lost & Found</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
  <!-- Bootstrap CSS and Icons -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
  <link rel="stylesheet" href="/found.css">
</head>
 <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Inter', sans-serif;
    }

    body {
      background: #f5f5f7;
      position: relative;
      overflow-x: hidden;
      min-height: 100vh;
    }

    /* Decorative background pattern */
    body::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 100%;
      background-image: 
        radial-gradient(circle at 20% 50%, rgba(59, 130, 246, 0.03) 0%, transparent 50%),
        radial-gradient(circle at 80% 80%, rgba(147, 197, 253, 0.04) 0%, transparent 50%);
      pointer-events: none;
      z-index: 0;
    }

    /* Diagonal grid pattern */
    body::after {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 100%;
      background-image: 
        repeating-linear-gradient(45deg, transparent, transparent 50px, rgba(0,0,0,0.01) 50px, rgba(0,0,0,0.01) 51px);
      pointer-events: none;
      z-index: 0;
    }

    header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 20px 60px;
      background: rgba(255, 255, 255, 0.8);
      backdrop-filter: blur(10px);
      -webkit-backdrop-filter: blur(10px);
      position: relative;
      z-index: 100;
    }

    .logo {
      font-weight: 700;
      font-size: 24px;
      color: #000;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .logo::before {
      content: '';
      width: 8px;
      height: 8px;
      background: #3b82f6;
      border-radius: 50%;
    }

    .logo::after {
      content: '';
      width: 8px;
      height: 8px;
      background: #60a5fa;
      border-radius: 50%;
    }

    nav ul {
      display: flex;
      list-style: none;
      gap: 40px;
    }

    nav ul li a {
      text-decoration: none;
      color: #000;
      font-weight: 500;
      font-size: 15px;
      transition: color 0.3s;
    }

    nav ul li a:hover {
      color: #3b82f6;
    }

    .auth-buttons {
      display: flex;
      gap: 12px;
    }

    .auth-buttons a {
      padding: 10px 24px;
      border-radius: 8px;
      text-decoration: none;
      font-weight: 500;
      font-size: 14px;
      transition: all 0.3s;
    }

    .login {
      color: #000;
      background: transparent;
    }

    .login:hover {
      color: #3b82f6;
    }

    .signup, .logout {
      background: #3b82f6;
      color: #fff;
    }

    .signup:hover, .logout:hover {
      background: #2563eb;
      transform: translateY(-1px);
      box-shadow: 0 4px 12px rgba(59, 130, 246, 0.3);
    }
</style>

<body>

 <header>
    <div class="logo">LoFo</div>
    <nav>
      <ul>
        <li><a href="/home">Home</a></li>
        <li><a href="/found">Found</a></li>
        <li><a href="/lost">Lost</a></li>
        <li><a href="/report">Check Reports</a></li>
      </ul>
    </nav>
    

    
      <div class="auth-buttons">
        <a href="/Logout" class="logout">Logout</a>
      </div>
    
  </header>

  <main class="container my-5">
    <h2 class="text-center mb-5 fw-bold text-primary">Available Items</h2>

    <div class="row g-4">
      <% for (let item of items) { %>
        <div class="col-sm-6 col-md-4 col-lg-3 d-flex align-items-stretch">
          <div class="card h-100 shadow-sm border-0 rounded-4">
            <img src="<%= item.img %>" class="card-img-top" alt="Item Image" style="height: 200px; object-fit: cover;">
            <div class="card-body d-flex flex-column">
              <h5 class="card-title fw-semibold mb-2">
                <%= item.title %>
              </h5>
              <h5 class="card-title fw-semibold mb-2">
                @<%= item.Author[0].name %>
              </h5>
              <p class="card-text text-muted mb-2">
                <%= item.describtion %>
              </p>

              <p class="mb-2">
                <i class="bi bi-geo-alt-fill text-danger me-1"></i>
                <span class="text-secondary">
                  <%= item.location %>
                </span>
              </p>

              <% if (item.describtionArr && item.describtionArr.length) { %>
                <div class="mb-3">
                  <% item.describtionArr.forEach(tag=> { %>
                    <span class="badge bg-info text-dark me-1">
                      <%= tag %>
                    </span>
                    <% }) %>
                </div>
                <% } %>

                  <div class="mt-auto d-flex gap-2">
                    <a href="#" class="btn btn-primary flex-grow-1">View Details</a>
                    <form action="/admin/delete/<%= item._id %>" method="POST" class="d-inline">
                      <button type="submit" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this listing?')">
                        <i class="bi bi-trash"></i>
                      </button>
                    </form>
                  </div>
            </div>
          </div>
        </div>
        <% } %>
    </div>
  </main>










</body>

</html>